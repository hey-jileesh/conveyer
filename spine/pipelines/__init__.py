"""`pipelines.*` namespace shipped in the spine wheel (D-2).

`PipelineSpecModel.transforms_module` is namespace-constrained to
`^pipelines\\.` (LLD §6.2, I-10). `pipelines.identity` arrives with the
identity exemplar (LLD §12.2, M3).
"""
