[project]
name = "conveyer"
version = "0.1.0"
description = "Conveyer — large-volume batch data processing lane (immutable-fact pipeline on Iceberg). See design/001_batch_data_processing_architecture.md."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false

[tool.uv.workspace]
members = ["ingestion"]
